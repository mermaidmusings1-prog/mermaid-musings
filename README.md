# Mermaid Musings — website

Static website for Mermaid Musings, deployed on Vercel.

- `index.html` — the whole site (single self-contained page, no build step).
- Hosting: Vercel project `mermaid-musings` (team Simplirun). Every push to `main`
  deploys automatically.

Current state: temporary "coming soon" placeholder. Colours and copy are a first
proposal and are pending Cecelia's confirmation of the brand and the offer.
