# SimpliRun — website

Marketing site for **SimpliRun** — "Business Operations Made Simple."
All-in-one automation (calls, scheduling, payments, follow-up, outreach) for solo and
small-team business owners.

- `index.html` — the entire site: one self-contained static page, no build step.
- Hosting: Vercel project `simplirun` (team Simplirun). Every push to `main` deploys automatically.
- Brand tokens (navy/mint palette, Poppins + Inter) come from Viktor's workspace brand spec.

Primary conversion goal: free AI receptionist setup → paid $4,500 SimpliRun Complete Build.

## TODO
- Replace the `mailto:` CTAs with a real lead form once GoHighLevel is set up.
- Add a logo when one exists (wordmark is currently type-only).
- Add proof/testimonials when available — never invent them.
